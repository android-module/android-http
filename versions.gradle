
def isRemote(){
    def what = false
    if(project.hasProperty("remoteDep")){
        what = remoteDep.toBoolean()
    }
    return what
}

ext{
    isRemote=this.&isRemote
}

project.ext{
    remoteDep=false
    android_http_version="1.0.1-rc2"
    android_http_impl_version="1.0.0-rc2"
    android_http_viewmodel_version="1.0.0-rc2"
}

